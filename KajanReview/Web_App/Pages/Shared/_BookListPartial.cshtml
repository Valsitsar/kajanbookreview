@using BusinessLogicLayer.Entities
@model Book
@{
}


<div class="">
    <div class="">
            <div class="">
                <img class="book-cover" src="@Model.CoverFilePath" alt="@Model.Title cover"/>
                <div>
                    @for (int i = 0; i < 5; i++) { <i class="bi bi-star"></i> }
                </div>
            </div>
            <div class="flex-right-container">
                <div class="title-author-lang-container">
                    <div class="short-text">
                        @Model.Title
                    </div>
                    <div class="short-text">
                        Example author
                    </div>
                    <div class="short-text">
                        @Model.Language
                    </div>
                </div>

                <div id="book-description">
                    @Model.Description
                </div>
            </div>
        <br />
        }
    </div>
</div>